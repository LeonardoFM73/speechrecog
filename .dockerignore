# Version control
.git
.gitignore

# Python
__pycache__/
*.pyc
venv/
*.egg-info/
.pytest_cache/

# Node
frontend/node_modules/
frontend/.next/
frontend/out/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Docker
# Note: Dockerfile.* excluded only for BACKEND build (uses Dockerfile.backend).
# For voicevox build, switch context or use a more specific pattern via
# `dockerfile` key + `context` key — explicitly referenced Dockerfiles are
# generally preserved by Docker.
docker-compose.yml
docker-compose.*.yml

# Docs
README.md
Summary.md
